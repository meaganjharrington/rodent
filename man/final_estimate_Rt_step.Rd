% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/final_estimate_Rt_step.R
\name{final_estimate_Rt_step}
\alias{final_estimate_Rt_step}
\title{Rt(t) with stepwise beta(t), fixed gamma; infer I0 and beta blocks via Metropolis-Hastings random walk MCMC
Using full odin2 -> dust2 -> monty workflow (deterministic likelihood)}
\usage{
final_estimate_Rt_step(
  incidence,
  N,
  gamma,
  beta_breaks = NULL,
  mcmc = list(n_steps = 6000, burnin = 0.5, proposal = NULL, seed = 4, n_rt_draws = 300),
  priors = list(mean_beta = log(0.3), sd_beta = 0.5, rw_sd_beta = 0.2, mean_I0 = log(10),
    sd_I0 = 0.5),
  inits = list(beta = 0.3, I0 = 10)
)
}
\arguments{
\item{incidence}{data.frame with columns: time (consecutive integers), cases >= 0.
Time can start at any integer, we keep it and set time_start to "first time - 1".}

\item{N}{Numeric scalar > 0, population size}

\item{gamma}{Numeric scalar > 0, fixed recovery rateR Immunity existing in population, default = 0 - add back in once baseline immunity functionality added!}

\item{beta_breaks}{Integer vector in ORIGINAL time units; mapped to indices internally}

\item{mcmc}{List: n_steps (6000), burnin (0.5), proposal (NULL -> diag(0.02^2)), seed (4),
n_rt_draws (300)}

\item{priors}{List: mean_beta=log(0.3), sd_beta=0.5, rw_sd_beta=0.2,
mean_I0=log(10), sd_I0=0.5}

\item{inits}{List: beta=0.3, I0=10}
}
\value{
list(samples, estimates, Rt_series, fit, diagnostics, model_used, blocks, fixed)
}
\description{
Rt(t) with stepwise beta(t), fixed gamma; infer I0 and beta blocks via Metropolis-Hastings random walk MCMC
Using full odin2 -> dust2 -> monty workflow (deterministic likelihood)
}
