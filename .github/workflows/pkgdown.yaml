
on:
  push:
    branches: [main]
  workflow_dispatch:

name: pkgdown

jobs:
  pkgdown:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true   # <-- Key line to fix ragg issues

      - uses: r-lib/actions/setup-pandoc@v2

      - name: Add mrc-ide R-universe
        run: |
          echo "options(repos = c(
            mrcide='https://mrc-ide.r-universe.dev',
            CRAN='https://cloud.r-project.org'
          ))" >> ~/.Rprofile

      - name: Install dependencies (odin2/dust2/monty/pkgdown)
        run: |
          options(repos = c(
            mrcide='https://mrc-ide.r-universe.dev',
            CRAN='https://cloud.r-project.org'
          ))
          install.packages(c(
            "pkgdown",
            "devtools",
            "odin2",
            "dust2",
            "monty"
          ))
          devtools::install()   # Install *your* package
        shell: Rscript {0}

      - name: Build pkgdown
        run: pkgdown::build_site()
        shell: Rscript {0}

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4.5.0
        with:
          branch: gh-pages
          folder: docs
