% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redo_fit.R
\name{redo_fit}
\alias{redo_fit}
\title{Estimate Rt(t) from daily cases via odin2/dust2/monty (SIR)
Maximum flexibility for technical user to make adjustments ?}
\usage{
redo_fit(
  incidence,
  N,
  I0 = NULL,
  gamma = 1/7,
  estimate_gamma = FALSE,
  use_negbin = FALSE,
  nb_size = 10,
  rho = 1,
  n_particles = 500,
  dt = 1,
  n_steps = 4000,
  burnin = NULL,
  chains = 3
)
}
\arguments{
\item{incidence}{data.frame with columns time (integer days) and cases (counts)}

\item{N}{population size}

\item{I0}{initial infections (default: inferred from first 5 days)}

\item{gamma}{fixed recovery rate per day (default 1/7)}

\item{estimate_gamma}{logical (default FALSE)}

\item{use_negbin}{logical; if TRUE, use NB observation in model (default FALSE here)}

\item{nb_size}{NB size if use_negbin (default 10)}

\item{rho}{reporting fraction if use_negbin (default 1)}

\item{n_particles}{PF particles (default 500)}

\item{dt}{discrete step width (default 1)}

\item{n_steps}{MCMC iterations (default 4000)}

\item{burnin}{burn-in iterations (default n_steps/2)}

\item{chains}{number of chains (default 3)}
}
\value{
list: samples, estimates, R_t (mean/lower/upper), fitted_incidence (PPC), diagnostics
}
\description{
Estimate Rt(t) from daily cases via odin2/dust2/monty (SIR)
Maximum flexibility for technical user to make adjustments ?
}
